/**
 * SheetService.js
 * Upserts normalized rows into the Activity tab, keyed on
 * (repo, type, number).
 *
 * Implemented in PHASE 3 — see PHASE3_INSTRUCTIONS.md.
 */

/**
 * @param {object[]} rows - normalized rows from Transformer.normalize
 */
function upsertActivityRows(rows) {
  throw new Error('Not implemented yet — see PHASE3_INSTRUCTIONS.md');
}
