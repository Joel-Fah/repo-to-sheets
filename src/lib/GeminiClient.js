/**
 * GeminiClient.js
 * Calls the Gemini API to summarize what changed since the last sync.
 *
 * Implemented in PHASE 4 — see PHASE4_INSTRUCTIONS.md.
 */

/**
 * @param {function} fetchFn
 * @param {string} apiKey
 * @param {string} diffSummaryPrompt - plain text describing what changed
 * @returns {string} short natural-language summary
 */
function summarizeActivity(fetchFn, apiKey, diffSummaryPrompt) {
  throw new Error('Not implemented yet — see PHASE4_INSTRUCTIONS.md');
}
